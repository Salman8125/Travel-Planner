# Bind port 4002. Spring Boot binds 0.0.0.0 by default (do NOT set server.address to loopback,
# or the service becomes unreachable from outside the container).
server.port=4002
spring.application.name=roost-backend
