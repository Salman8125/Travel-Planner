# Backend base URL — baked at BUILD time by Vite. The browser is OUTSIDE the docker
# network, so this points at the PUBLISHED host port, never the compose service name.
VITE_API_URL=http://localhost:4004
